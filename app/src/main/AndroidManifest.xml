<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:dist="http://schemas.android.com/apk/distribution"
    package="com.jetpeck.demo">

    <dist:module dist:instant="true" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="false"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".basic.lifecycles.SystemLifeCyclesActivity"
            android:screenOrientation="portrait" />
        <activity
            android:name=".basic.lifecycles.CustomLifeCyclesActivity"
            android:screenOrientation="portrait" />
        <activity android:name=".basic.viewmodel.ViewModelActivity" />
        <activity android:name=".basic.livedata.SysLiveDataActivity" />
        <activity android:name=".basic.livedata.CustomLiveDataActivity" />
    </application>

</manifest>